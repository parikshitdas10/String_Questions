//Java Program to Check if a string is a valid shuffle of two distinct strings
import java.util.*;
public class StringQ6 {
    static Scanner sc=null;
    public static void main(String[] args) {
        sc= new Scanner(System.in);
        ArrayList<String> check=new ArrayList<>();
        System.out.println("Enter two distinct Strings: ");
        String str1=sc.nextLine();
        String str2=sc.nextLine();
        System.out.println("Enter no. ofshuffles to be checked: ");
        int n=sc.nextInt();
        getArray(check,n);
    }
    static void getArray(ArrayList<String> check, int n){
        for(int i=0;i<n;i++){
            
        }
    }
}
